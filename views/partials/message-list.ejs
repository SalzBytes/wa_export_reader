<% let lastDate=null; %>
<% messages.forEach(m=> { %>
<% if (m.date !==lastDate) { %>
<%- include('date-separator', { date: m.date }) %>
<% lastDate=m.date; %>
<% } %>
<%- include('message', { m }) %>
<% }) %>